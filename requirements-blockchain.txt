# MySocial wallet / chain signing (works on Python 3.10–3.14)
eth-account>=0.9.0
ecdsa>=0.18.0
cryptography>=41.0.0
pynacl>=1.5.0
mnemonic>=0.21

# Optional: secp256k1/secp256r1 mnemonic HD paths only (requires coincurve; Python 3.10–3.13)
# pip install bip-utils>=2.9.0

# MySocial fullnode gRPC (EventService.ListAuthenticatedEvents)
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0
