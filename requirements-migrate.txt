# Minimal dependencies for `alembic upgrade head` (no ML / blockchain stack).
# Use: pip install -r requirements-migrate.txt
alembic>=1.13.0
sqlalchemy>=2.0.23
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
pgvector>=0.2.0
